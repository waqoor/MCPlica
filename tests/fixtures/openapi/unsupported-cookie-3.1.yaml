openapi: 3.1.0
info:
  title: Unsupported cookie parameter
  version: 1.0.0
servers:
  - url: https://unsupported.example.com
paths:
  /cookie:
    get:
      parameters:
        - name: session
          in: cookie
          schema: {type: string}
      responses:
        '200': {description: OK}
